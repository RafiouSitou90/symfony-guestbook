<ul>
    {% for conference in conferences %}
        <li>
            <a href="{{ path('app_conference_show', { slug: conference.slug }) }}">
                {{ conference }}
            </a>
        </li>
    {% endfor %}
</ul>

